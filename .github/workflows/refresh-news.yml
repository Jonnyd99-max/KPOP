name: Refresh KPOP buzz

on:
  schedule:
    - cron: "17 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Pull fresh headlines from multiple searches
        run: python scripts/refresh_news.py
      - name: Publish changed feed
        run: |
          if git diff --quiet -- news.json; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add news.json
          git commit -m "Refresh latest K-pop buzz"
          git push

